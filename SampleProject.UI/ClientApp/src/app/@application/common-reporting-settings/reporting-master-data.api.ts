export enum ReportingMasterDataApi{
  reportingSettings = 'reporting-master-data',
  segments = 'segments',
  segmentFields = 'segment-fields',
  dataMapping = 'data-mapping',
  update = 'segment-catalogue-type'
}
